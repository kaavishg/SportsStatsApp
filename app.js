function selectSport(sport) {
    const dataContainer = document.getElementById('data-container');
    dataContainer.innerHTML = `<p>Loading ${sport} stats...</p>`;
    // Insert logic here to fetch and display stats.
  }
  